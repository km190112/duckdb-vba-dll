[package]
name = "duckdb-r"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "DuckDB VBA DLL — ① 読み取り専用"

[lib]
name = "duckdb_r"
crate-type = ["cdylib"]

[dependencies]
duckdb-core = { workspace = true }
